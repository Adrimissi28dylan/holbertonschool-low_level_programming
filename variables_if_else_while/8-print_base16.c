#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int n = '0';

	for (n = '0' ; n <= '9' ; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
